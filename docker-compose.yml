version: "3"
services:
  app:
    image: node:latest
    volumes:
      - ./:/app
    working_dir: /app
    # depends_on:
    #   - mongo
    environment:
      NODE_ENV: development
    ports:
      - 8080:8080
    command: npm run dev
  # mongo:
  #   image: mongo
  #   expose:
  #     - 27017
  #   volumes:
  #     - ./data/db:/data/db